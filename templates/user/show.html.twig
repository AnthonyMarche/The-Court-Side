{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    {% if user.id == app.user.id %}
        {# --- MODALE CONFIRMATION SUPPRESSION --- #}

        <div class="modal fade" id="deleteProfileModal" tabindex="-1" role="dialog" aria-labelledby="deleteProfileModal"
             aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="deleteProfileTitle">Suppression du compte utilisateur</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        Êtes-vous bien sûr de vouloir supprimer votre compte ? Si vous confirmez, vous n'aurez plus
                        accès aux vidéos réservées aux abonnés.
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-warning" data-dismiss="modal">Annuler</button>
                        {% include 'user/_delete_form.html.twig' %}
                    </div>
                </div>
            </div>
        </div>

        {# --- RESUME PROFIL UTILISATEUR --- #}
        <div class="container show-user">
            <h1 class="text-center">Profil utilisateur</h1>
            <div>
                <div class="p-3 show-user-table">
                    <table class="table">
                        <tr>
                            <th>Adresse email</th>
                            <td>{{ app.user.email }}</td>
                        </tr>
                        <tr>
                            <th>Mot de passe</th>
                            <td>* * * * * * *</td>
                        </tr>
                        <tr>
                            <th>Nom d'utilisateur</th>
                            <td>
                                {% if app.user.username %}
                                    {{ app.user.username }}
                                {% else %}
                                    <p class="font-italic">
                                        Vous pouvez ajouter un nom d'utilisateur en modifiant votre profil.
                                    </p>
                                {% endif %}
                            </td>
                        </tr>
                        <tr>
                            <th>Abonnement newsletter</th>
                            <td>
                                {% if not app.user.newsletter %}
                                    <p class="font-italic">
                                        Vous n'êtes pas encore abonné à la newsletter. Vous pouvez vous y abonner en modifiant votre profil.
                                    </p>
                                {% else %}
                                    <p class="font-italic">
                                        Vous êtes abonné à la newsletter. Vous pouvez vous désabonner en modifiant votre profil.
                                    </p>
                                {% endif %}
                            </td>
                        </tr>
                    </table>
                </div>
                <div class="container d-flex justify-content-around p-4">
                    <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-warning">Modifier mon
                        profil</a>
                    <!-- Button trigger modal -->
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteProfileModal">
                        Supprimer mon compte
                    </button>
                </div>
            </div>
        </div>
    {% else %}
        <div class="container d-flex justify-content-center mt-4">
            <div class="card">
                <div class="card-body d-flex flex-column">
                    <p class="card-text font-italic">Vous n'êtes pas autorisé à accéder à cette page.</p>
                    <a href="{{ path('app_home') }}" class="btn btn-success">Retourner vers la page d'accueil</a>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
