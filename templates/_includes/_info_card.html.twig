<div class="info-card card">
    <div class="info-card-body card-body text-center bg-light">
        <h5 class="card-title mt-5">{{ title }}</h5>
        <p class="card-text">{{ text }}</p>
        {% if buttons == 'true' %}
            <div class="info-card-buttons-container d-flex justify-content-center mt-5">
                <a href="#" class="btn btn-outline-info">S'inscrire</a>
                <a href="{{ path('app_user_login') }}" class="btn btn-primary">Se connecter</a>
            </div>
        {% endif %}
    </div>
</div>
