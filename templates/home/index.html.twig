{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

    <!-- home carousel -->

    <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100 home-carousel-image" src="{{ asset('build/images/home-carousel-paddle.jpg') }}"
                     alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 home-carousel-image" src="{{ asset('build/images/home-carousel-ski.jpg') }}"
                     alt="Second slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 home-carousel-image" src="{{ asset('build/images/home-carousel-f1.jpg') }}"
                     alt="Third slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

    <!-- videos -->

    <div class="container-fluid p-5 py-3 home-main-container">
        <div class="home-section-head d-flex justify-content-between p-0 mb-0">
            <h3 class="section-name">Les videos publiées récemment</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-primary">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row">
            {% for video in latestVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {'id' : video.id}) }}" class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <img src="{{ asset('build/images/image-private-video.jpg') }}" class="img-fluid mt-2"
                                 alt="image-private-video">
                        {% else %}
                            <video src="{{ asset('build/images/test-video-teaser.mp4') }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">
                                    {% if video.user.username %}
                                        {{ video.user.username }}
                                    {% else %}
                                        {{ video.user.email }}
                                    {% endif %}
                                </div>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
        <div class="home-section-head d-flex justify-content-between mt-5">
            <h3 class="section-name">Les vidéos les plus vues</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-primary">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row">
            {% for video in popularVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {'id' : video.id}) }}" class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <img src="{{ asset('build/images/image-private-video.jpg') }}" class="img-fluid mt-2"
                                 alt="image-private-video">
                        {% else %}
                            <video src="{{ asset('build/images/test-video-teaser.mp4') }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">
                                    {% if video.user.username %}
                                        {{ video.user.username }}
                                    {% else %}
                                        {{ video.user.email }}
                                    {% endif %}
                                </div>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>

    <!-- middle image-->

    <div class="home-middle-image">
        <img src="{{ asset('build/images/home-middle-image.jpg') }}" class="shadow-lg img-fluid home-middle-image" alt="home middle image">
    </div>
    <div class="container-fluid p-5 py-3 home-main-container">


        <div class="home-section-head d-flex justify-content-between">
            <h3 class="section-name">Tennis</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-primary">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row">
            {% for video in tennisVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {'id' : video.id}) }}" class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <img src="{{ asset('build/images/image-private-video.jpg') }}" class="img-fluid mt-2"
                                 alt="image-private-video">
                        {% else %}
                            <video src="{{ asset('build/images/test-video-teaser.mp4') }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">
                                    {% if video.user.username %}
                                        {{ video.user.username }}
                                    {% else %}
                                        {{ video.user.email }}
                                    {% endif %}
                                </div>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
        <div class="home-section-head d-flex justify-content-between mt-5">
            <h3 class="section-name">Les vidéos réservées aux inscrits</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-primary">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row">
            {% for video in registerVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {'id' : video.id}) }}" class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <img src="{{ asset('build/images/image-private-video.jpg') }}" class="img-fluid mt-2"
                                 alt="image-private-video">
                        {% else %}
                            <video src="{{ asset('build/images/test-video-teaser.mp4') }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">
                                    {% if video.user.username %}
                                        {{ video.user.username }}
                                    {% else %}
                                        {{ video.user.email }}
                                    {% endif %}
                                </div>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>

    <!-- footer -->

    <div class="home-footer-sponsor-logo-container d-flex justify-content-around">
        <img class="home-footer-sponsor-logo" src="" alt="sponsor logo">
        <img class="home-footer-sponsor-logo" src="" alt="sponsor logo">
        <img class="home-footer-sponsor-logo" src="" alt="sponsor logo">
    </div>
    <div class="d-flex justify-content-around align-items-center home-footer-middle-line">
        <div class="home-footer-website-logo-container d-flex justify-content-around">
            <img class="home-footer-website-logo img-fluid" src="{{ asset('build/images/TCS favicon.png') }}" alt="website logo">
        </div>
        <div class="home-footer-links d-flex">
            <a class="text-dark" href="">ASSE.FR</a>
            <p> | </p>
            <a class="text-dark" href="">COOKIES</a>
            <p> | </p>
            <a class="text-dark" href="">MENTIONS LEGALES</a>
            <p> | </p>
            <a class="text-dark" href="">POLITIQUE DE CONFIDENTIALITE</a>
        </div>
        <div class="home-footer-social-links d-flex">
            <a href="#" class="fa fa-facebook footer-social-logo"></a>
            <a href="#" class="fa fa-twitter footer-social-logo"></a>
            <a href="#" class="fa fa-instagram footer-social-logo"></a>
        </div>
    </div>
    <div class="home-footer-description">
        <p class="text-center">POWERED BY ON REWIND, AN EMG COMPANY<br>APP VERSION:1.2.0 / UI VERSION:3.1.1</p>
    </div>
{% endblock %}


{#
{% for video in videos %}
    <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
        <a href="" class="card card-video border-0 bg-transparent mb-4 home-video-link">
            {% if video.isPrivate and not app.user %}
                <img src="{{ asset('build/images/image-private-video.jpg') }}" class="img-fluid mt-2" alt="image-private-video">
            {% else %}
                <img src="{{ asset('build/images/carousel-basket.jpg') }}" class="img-fluid mt-2" alt="image-{{ video.title }}">
            {% endif %}
            <div class="card-video-details d-flex mt-2">
                <div>
                    <h4 class="home-video-title">{{ video.title }}</h4>
                    <div class="home-video-details">
                        {% if video.user.username %}
                            {{ video.user.username }}
                        {% else %}
                            {{ video.user.email }}
                        {% endif %}
                    </div>
                    <div class="home-video-details">{{ video.numberOfView }} vues • publiée le {{ video.createdAt|date("d/m/Y") }}</div>
                </div>
            </div>
        </a>
    </div>
{% endfor %}
#}
