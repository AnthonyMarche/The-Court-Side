{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

    <!-- home carousel -->

    <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100 home-carousel-image"
                     src="{{ asset('build/images/home-carousel-paddle.jpg') }}"
                     alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 home-carousel-image" src="{{ asset('build/images/home-carousel-ski.jpg') }}"
                     alt="Second slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100 home-carousel-image" src="{{ asset('build/images/home-carousel-f1.jpg') }}"
                     alt="Third slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

    <!-- videos -->

    <div class="container-fluid p-5 py-3 home-main-container">
        <div class="home-section-head d-flex justify-content-between p-0 mb-0">
            <h3 class="section-name">Publiées récemment</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-light see-more-button">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row video-row">
            {% for video in latestVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {slug : video.slug}) }}"
                       class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                   class="w-100 mt-2">
                            </video>
                        {% else %}
                            <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                {% if video.isPrivate and not app.user %}
                                    <i class="fa fa-lock"> Réservée aux inscrits</i>
                                {% endif %}
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
        <div class="home-section-head d-flex justify-content-between mt-5">
            <h3 class="section-name">Les plus vues</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-light see-more-button">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row">
            {% for video in popularVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {slug : video.slug}) }}"
                       class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                               class="hover-to-play w-100 mt-2">
                        </video>
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                {% if video.isPrivate and not app.user %}
                                    <i class="fa fa-lock"> Réservée aux inscrits</i>
                                {% endif %}
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>

    <!-- middle image-->

    <div class="home-middle-image-container">
        <img src="{{ asset('build/images/home-middle-image.jpg') }}" class="shadow-lg img-fluid home-middle-image"
             alt="home middle image">
    </div>
    <div class="container-fluid p-5 py-3 home-main-container">


        <div class="home-section-head d-flex justify-content-between">
            <h3 class="section-name">Les plus likées</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-light see-more-button">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row video-row">
            {% for video in moreLikedVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {slug : video.slug}) }}"
                       class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <div class="d-flex justify-content-center lock-image-container mt-2">
                                <img src="{{ asset('build/images/locklogo.png') }}"
                                     class="img-fluid w-100 mt-2 lock-image d-flex align-self-center"
                                     alt="image-private-video">
                                <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                       class="hover-to-play w-100 private-video">
                                </video>
                            </div>
                        {% else %}
                            <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
        <div class="home-section-head d-flex justify-content-between mt-5">
            <h3 class="section-name">Réservées aux inscrits</h3>
            <div></div>
            <a href="">
                <button type="button" class="btn btn-outline-light see-more-button">VOIR PLUS</button>
            </a>
        </div>
        <div class="home-section-line mb-2"></div>
        <div class="row video-row">
            {% for video in registerVideos %}
                <div class="col-12 col-md-6 col-lg-4 col-xl-3 single-video">
                    <a href="{{ path('app_watch', {slug : video.slug}) }}"
                       class="card card-video border-0 bg-transparent mb-4 home-video-link">
                        {% if video.isPrivate and not app.user %}
                            <div class="d-flex justify-content-center lock-image-container mt-2">
                                <img src="{{ asset('build/images/locklogo.png') }}"
                                     class="img-fluid w-100 mt-2 lock-image d-flex align-self-center"
                                     alt="image-private-video">
                                <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                       class="w-100 private-video">
                                </video>
                            </div>
                        {% else %}
                            <video src="{{ asset(video.teaser) }}" type="video/mp4" loop muted
                                   class="hover-to-play w-100 mt-2">
                            </video>
                        {% endif %}
                        <div class="card-video-details d-flex mt-2">
                            <div>
                                <h4 class="home-video-title">{{ video.title }}</h4>
                                <div class="home-video-details">{{ video.numberOfView }} vues • publiée
                                    le {{ video.createdAt|date("d/m/Y") }}</div>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
